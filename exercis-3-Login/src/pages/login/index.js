import React, { Component } from "react";

class LoginAuth extends Component {
  state = {};
  render() {
    return (
      <div>
        <h1>Login account</h1>
        <button>Go to regis</button>
        <button>Go to dashboard</button>
      </div>
    );
  }
}

export default LoginAuth;
